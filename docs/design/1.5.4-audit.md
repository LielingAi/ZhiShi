# 1.5.4 全仓代码质量审计报告（2026-08-31）

> 六个子审计面（loop / server 核心 / server 其余 / CLI / GUI / shared+scripts）并行审计结果汇总。
> 共 **161 条**：死代码 60+ / 无效链路 20+ / 逻辑问题 20+ / 注释漂移 30+。
> 所有「零引用」结论均经全仓 grep 验证（含测试、scripts、src-tauri）。
> **本报告供用户逐条审计拍板，确认后才修复。**

---

## A. 真 bug（逻辑问题——影响功能，建议优先决策）

### A1. 高置信

| # | 位置 | 问题 | 影响 |
|---|---|---|---|
| A1-1 | `session.ts:307-318` | `markLoopSessionCompacted` 重建 meta 时**不带 tokenCalibration**（其余三个 meta 重写点都保留） | **1.5.3 自伤**：每次压缩触发就把学到的校准系数清掉，判定回落 calibration=1——恰是压缩后最容易误判的阶段 |
| A1-2 | `chat-engine.ts:1503` | `invokePiSession` 直接写 `this.currentTurnUserMessageId = undefined`，与自身注释「全程不碰引擎单例状态」矛盾 | 交互 turn 与 invoke 并发时，进行中交互轮的档案来源锚被清空，且不恢复 |
| A1-3 | `harvest.ts:126-137` | 指针卡**不含 jsonl 行区间**（注释承诺含），模型从卡上拿不到 lines 参数 | recall 链路断一环：必须先 recall({ref}) 才能间接拿到区间 |
| A1-4 | CLI `zhishi.ts:1402` | 全局 `--port` 旗标在路由分发前无条件覆盖 sidecar 端口；`env add --kind ssh --port 2222` 的 ssh 端口被误当 sidecar 端口 | `env add --port` 必然 ECONNREFUSED 失败（文档化的用法必炸） |
| A1-5 | CLI `zhishi.ts:469-470` | `domain list` 打印读 `d.skills`，服务端 1.5.1 起不返回该字段 | 每行永远打印 `skills:无`，真正存在的 `subagents` 从不展示 |
| A1-6 | `package.json:52` | npm `version` 钩子只 git add tauri.conf.json + Cargo.toml，**漏 src/shared/constants.ts**（sync-version 注释明说这是 1.4.8/1.4.9 漏同步教训，钩子本身却没修） | 下次 `npm version` 流程 GUI_VERSION 漏同步复现 |

### A2. 中置信

| # | 位置 | 问题 | 影响 |
|---|---|---|---|
| A2-1 | `chat-engine.ts:1300-1306`（invoke 同构 :1573-1582） | 校准学习分子=当轮最后一次 LLM 调用实测（含当轮增量），分母=起跑前 history 估算（不含增量）——口径不一致，比值恒 >1 | 工具密集轮顶到钳位 6，下一轮压缩提前触发 |
| A2-2 | `chat-engine.ts:1167 / 1512` | 专家注入的 history 是当轮 prompt **之前**的历史，lastUserTextOf 取到上一条用户消息 | 注入锚点滞后一轮；首轮永不注入 |
| A2-3 | `subagent.ts:94` + `context-manager.ts:430` | 子 loop 工具集只有 env_exec（无 recall），但其 stub 兜底文本写「需要时用 recall 工具取回」 | 模型照做即幻觉调用被 boundary 拦 |
| A2-4 | `index.ts:1423` | 跨线决策应答 `await injectPiDecision` → invokePiSession 不传 timeoutMs → 同步跑完整个 agent turn（分钟级）才返回 HTTP 响应 | 决策应答请求挂起分钟级 |
| A2-5 | `pi-provider.ts:65/224` | `maxOutputTokensParamName` 声明+透传但 buildLoopModel 从不读 | 需要 `max_completion_tokens` 的 OpenAI 兼容 provider 静默用错参数名（SDK 残留字段） |
| A2-6 | `auto-run.ts:135-147` | `verdictRequestOfRecord` 对每个 criteria 硬填 `refs: []` | 断线恢复路径的终审弹窗 refs 列永远渲染空 |
| A2-7 | GUI `Drawer.tsx:32-39` | 搜索在高亮后的 HTML 上做 indexOf 切片（搜 "flag" 命中 `hl-flag` class 名） | 截断标签产出破 HTML |
| A2-8 | GUI `useGuiStore.ts:2504-2507` | `restoreEnvSelection` 无条件 `[key]: emptySession()` 覆盖既有会话槽（锚路径用 `??` 保留） | 兜底路径清掉目标线本地状态，与「不丢任何线状态」原则矛盾 |
| A2-9 | CLI `zhishi.ts:1247-1252` | `expert review --json` 非 TTY 路径无视 jsonMode 直接输出人类格式 | `--json` 契约在该命令不成立 |

### A3. 低置信

| # | 位置 | 问题 |
|---|---|---|
| A3-1 | `auto-run.ts:1105-1109` | 预算续命恢复 running 分支漏 `persist()`（其余四个恢复分支都有）；崩溃窗口内盘上是旧状态 |
| A3-2 | GUI `useGuiStore.ts:2278` | Esc 链 verdictOpen 判定（verdict 非空即算开）与模态渲染条件（还要求 awaiting-verdict）不一致；竞态时 Esc 被静默吞 |
| A3-3 | GUI `reducer.ts:535-536` | `chat:message-complete` 无当前块时 early return 不更新 phase，窄窗口滞留 'running' |
| A3-4 | `skills-config.ts:166-168` | `syncToolSkills` 覆盖路径 rm+cp 无备份回滚（同文件 recipes 路径坚持回滚） |
| A3-5 | `cron/routes.ts:535-637` | `effectiveRuntimeConfig.model` 三处精心构造但全函数只读 permissionMode——write-only 死存储，注释承诺空转（M4c 残留） |
| A3-6 | CLI `cli-args.ts:16` | `term open --env` 落在 repeatable 集合，重复传拼成 `'a,b'` 非法 envTag 无告警 |
| A3-7 | `scripts/sync-version.js` | Cargo.toml/constants.ts 正则 replace 不校验是否真替换成功，失配静默打印 ✓ |
| A3-8 | `scripts/smoke.mjs:67` | `--only` 缺值时静默跑全量（用户以为只跑一个套件） |

---

## B. 死代码（删除面——按组决策）

### B1. server 核心：30+ 条死路由（高置信，约占 index.ts 路由表一半）

- **cron 残留**：`/cron/execute` + `/cron/check-completion`（Rust 只调 execute-sync；check-completion 即使被调也查错会话）
- **sessions 残留**：`/sessions/:id/since/:lastMessageId`、`/sessions/:id/stats`、POST `/sessions`、`/api/generate-session-title`（标题已全走后端钩子）
- **chat 残留**：POST `/chat/queue`、`/chat/queue/force`（GUI 只用 status/cancel）
- **SDK 时代残留**：`/api/global-stats`、`/api/task/poll-background`、`/api/session/messages`（读已删 SDK 的会话文件）
- **browser dev-mode 残留**：`/agent/switch`、`/agent/upload`、`/api/image`、`/api/audio`、`/api/attachment/tool/*`、`/api/attachment/*`（引用的 `zhishi://attachment` 协议在 src-tauri 已不存在）
- **IM-router 残留桩**：`/api/provider/set`、`/api/session/permission-mode`（纯 no-op 桩）、`/api/session/config`、`/api/model/set`、`/api/agents/set`
- **rules/commands/agents 文件管理面**（~1000 行）：`/api/rules*`、`/api/command-items`、`/api/command-item/*`、`/api/agent/sync-check|sync-from-claude|create|:name`、`/api/agents/workspace-config`、`/api/agents/enabled`（GUI 只用 GET /api/agents 做 @ 补全）
- **refs 大值外溢链路整链死**：`maybeSpill` 零调用 → refs 永不创建 → `/refs/:id` 端点 + `startRefsGc` 空转（**注意：CLAUDE.md:380 红线表仍宣称 >256KB 走 maybeSpill——删链路前要么先确认 SSE 大 payload 无需保护，要么恢复保护**）
- **零散**：`/api/assets/qr-code`、`/api/unified-log`、`/api/logs/export`、`/api/provider/verify`、`/api/proxy/set`、`/debug/logger`、`/health/live`、`/health/functional`
- **SSE `config:changed` 广播无消费者**（8 处广播点，GUI/CLI/Rust 零监听）
- **死函数**：`runRecipeToolCheck`（admin-api.ts:2234，已被 runEnvProbeWithCapabilities 取代）；`isStdioBroken/markStdioBroken` 再导出（index.ts:130）；`liveStreamingMessage`/`liveSessionState` 恒 null 残留（sessions.ts）；`SendMessagePayload.backgroundAgentPermissionMode` 字段从不读；`CronExecutePayload.isFirstExecution` Rust 每 tick 发但无人读

### B2. server utils：14 个僵尸模块（高置信，主体是 M4c 删 SDK 后的决策核）

整模块生产零引用（仅自身单测，单测全绿制造了还活着的假象）：
`workspace-files.ts`、`environment/boundary.ts`（注意：与 loop/boundary.ts 同名不同物）、`turn-abort.ts`、`fork-remap.ts`、`rewind-anchor.ts`、`inflight-terminal.ts`、`inactivity-watchdog.ts` + `watchdog-auto-resume.ts`（看门狗从未接线）、`plan-mode-gate.ts`（**注意：plan 权限模式仍是活的 UI 值但无拦截——删门前先决策 plan 模式要不要拦截**）、`imageResize.ts`（连带 sharp 依赖与构建 staging 在服务死代码）、`safe-file-path.ts`、`gitignore.ts`、`kill-with-escalation.ts`、`npm-prefix-env.ts`、`session-materialization.ts`

活文件内死导出：`agent-session.ts` 的 waitForSessionIdle/getAgentDir/hasInitialPromptSet；`SessionStore.ts` 的 appendSessionMessage/saveAttachment；`memory/store.ts` 的 getEntry/listArchive；`admin-config.ts` saveProjects；`shell.ts` ensureShellPath；`platform.ts` getHomeDir；`large-value-store.ts` 整组（maybeSpill/fetchRef/clearSessionRefs/stopRefsGc——与 B1 refs 链路同源）；`cancellation.ts` 三个导出；`model-aliases.ts` modelAliasEnvChangesForModel；`distill-research.ts` hasResearchDistilledContent

**无效链路**：terminal envTag 写了没人读（registerTerminalEnvTag 写入，唯一读取方随 environment/boundary.ts 死亡）

### B3. loop 死代码（高置信）

- `getPiLogLines()` 恒空壳 + `chat:logs` 全链无消费者（GUI reducer 明确注释不消费）
- `sendPiChatMessageAndWait` 生产零调用 + 连带 `turnAnswerByQueueId` 表只写不读（有单测，需同删）
- compaction.ts 转发导出块 8 符号中 6 个全仓零引用（注释称「保持既有引用兼容」已不成立）；连带 context-manager expandToolPairs 注释失真
- `CorrectInput.statusOverride` 死参数；archive `'doubtful'` 状态无写路径可达
- BoundaryAskKind 四类只有 host-write 有生产者（其余三类可能是规划占位——**决策：删还是留**）

### B4. CLI 死代码

- `cli-args.ts` repeatable 集合的 args/headers/var（MCP 删除残留，测试 fixture 还在用已删的 `mcp add` 形态）；短旗标 `-p` 无消费方
- zhishi.ts:46 悬空注释残迹；:49 注释说 exit-1 实际 exit(2)；:1615 注释块错位；TOP_HELP 缺 memory 命令条目

### B5. GUI 死代码（高置信 12 项 + 死字段 7 项）

死导出：activeSessionKey、BoundaryAskKind、steeringToast、thinkingTotalSeconds、hasBoundaryAsk、canStartEntry、archiveConfirmedHypotheses、isSwitchable、tauri-env.ts 整模块、api.ts expertRm（服务端路由在但 GUI 无删除入口——**决策：删 API 客户端函数还是补 GUI 删除入口**）、TERM_CLOSE_* 常量、SysItem 及三个 `case 'sys'` 不可达分支

写了没人读的字段：DrawerState.signal、QueueItem.addedAt、BgTaskEntry/SubagentEntry 的 startedAt/finishedAt、ThinkingDetail/ToolDetail.startedAt、TurnMeta.model、SessionMetaRow.titleSource/favorite

死样式：`.set-nav-item.disabled`、`.sr-status.bad`（MCP 残留）

### B6. shared + scripts 死代码（最大的一块：12 个死文件 + ~600 行死链）

死文件（高置信）：`skillsTypes.ts`、`dir-types.ts`、`types/ipc.ts`、`types/planMode.ts`、`types/chat.ts`（165 行 25 导出）、`types/stream.ts`、`types/tool-attachment.ts`、`types/task.ts`、`types/askUserQuestion.ts`、`types/trust.ts`、`fileTypes.ts`、`i18n.ts`、`tool-risk.ts`、`__fixtures__/dirtyConfig301.json`

runtime.ts 两条整链死（~600 行）：权限模式链（getRuntimePermissionModes 零调用 → CC_/GEMINI_/CODEX_PERMISSION_MODES/CC_MODELS 等）+ 诊断链（RuntimeDiagnostics 零引用 → 9 个关联类型）

config-types.ts 死导出一批（PERMISSION_MODES/getModelDisplayName/getProviderModels/getEffectivePrimaryModel/isVerifyExpired/PROXY_DEFAULTS/isValidProxyHost/ProjectSettings/getEffectiveModelAliases/DEFAULT_CONFIG/DEFAULT_SUMMON_ACCELERATOR 等）

slashCommands.ts skill 残余群（SkillFrontmatter/SlashCommand/BUILTIN_SLASH_COMMANDS/parseSkillFrontmatter/parseFullSkillContent/serializeSkillContent）

constants.ts：PENDING_SESSION_PREFIX 系列；**CUSTOM_EVENTS 全部 7 个 DOM 事件名零 dispatch 零 listener（整套事件桥已死）**

其他：AgentDetail、resolveEffectiveConfig、compareVersions、RENDERER_PERF_PHASE/formatPerfLine、_exhaustiveRuntimeCheck 的 export

### B7. 无效链路（跨面）

- `mcp_servers` 字段（system.ts:8）——协议镜像字段，删除收益低，**建议留**
- `plan` 权限模式：UI 活值但无拦截（见 B2 plan-mode-gate）

---

## C. 注释与实现漂移（低风险，随修）

高置信：
- chat-engine.ts:4-5 头注释「缺省 sdk 引擎」——SDK 已删，恒 pi
- chat-engine.ts:56「fork 不在 pi 路径」——forkPiChat 已实现接路由
- chat-engine.ts:999「MCP 每 turn 热读」——MCP 已删
- compaction.ts 头注释 :15-18/:57-60 描述已删除的 usage 锚路径；context-manager.ts:147/:406 同
- harvest.ts/compaction.ts 头注释承诺指针卡含行区间（实际不含，见 A1-3）
- bg-exec.ts:226-230 头注释「不做进程组杀」与实现（组杀优先）直接冲突
- cron/routes.ts:48-56 孤儿 JSDoc 描述已删函数
- index.ts:181-187 注释指向不存在的 /api/title-generate
- index.ts:3184 注释称 serveStatic 服务 React dist（实际只服务 placeholder）
- routes/sessions.ts:293/716 注释引用不存在的 zhishi://attachment 协议
- admin-api.ts:727 vs 731 同函数自相矛盾（注记说无需重载，hint 说会 restart）
- agent-session.ts:24/423 头注释列已删功能（waitForSessionIdle/scanSkills）
- slashCommands.ts:1-3 头注释宣称支持 skills；:44 指向不存在的 src/renderer
- large-value-store.ts:13 注释称 clearSessionRefs 在 session-end 调用（无人调）
- terminal-tags.ts / registry.ts 头注释描述已死链路
- index.ts:656 注释称有 10 分钟看门狗（从未接线）
- environment/boundary.ts:43 注释称供 SDK hooks 用（已删）

中低置信：
- loop.ts:9「convertToLlm 恒等」与实际过滤矛盾
- chat-engine.ts:1166「chars/4」实际系统提示按 chars/2
- index.ts:676 SILENT_PATHS 注释为死路径服务；:776 deferred-gate 注释引用已删 MCP
- admin-api.ts:1142 注释引用不存在的 buildClaudeSessionEnv；:1206 节注释称 MCP tools
- index.ts:2102 节头残留 SLASH COMMANDS/CLAUDE.md API 字样
- boundary-ask.ts:88-95 两段重复 JSDoc 叠加
- cron/routes.ts:57/487 注释引用已删 enqueueUserMessage + 重复项目符号
- useGuiStore.ts:329/1631 注释「Stream 消费后即清」实际只读不清（功能无害）
- styles.css:384/398 MCP 注释残留；Modal.tsx:59 注释「内部去抖」不实（靠服务端幂等兜底）
- archive.ts:117 注释「HistoryPanel 分组用」实际未引用
- tauri-env.ts:7-9 头注释描述 skills 导入（层已删 + 模块死）

---

## D. 子审计员验证过的「非问题」（去重记录）

- CLI 发出的全部 admin 路由在服务端均有注册，无死路由（除 B1 所列）
- GUI SSE 消费面无「GUI 监听但服务端不发」的断链
- api.ts 除 expertRm 外全部导出均有消费者
- 1.5.3 新压缩中枢（contextUsage/harvest/recall）实现与注释一致、链路完整
- environment/expert/intel/report/memory/distill/domains/migrations/agents 模块级引用全部有活链路
